apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sre-ingress
  namespace: sre-test
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    # Replace <PUBLIC_IP> with your cluster's public IP (e.g., 203.0.113.10)
    # Example: sre-go.203.0.113.10.nip.io
    - host: sre-go.<PUBLIC_IP>.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sre-go
                port:
                  number: 80
    - host: sre-node.<PUBLIC_IP>.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sre-node
                port:
                  number: 80
